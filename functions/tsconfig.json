{
  "//": "Scoped to Cloudflare Pages Functions. Worker globals are kept out of the site's own tsconfig so they cannot clash with browser DOM types in src/.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types"],
    "lib": ["ES2022"],
    "moduleResolution": "bundler",
    "module": "ESNext",
    "target": "ES2022",
    "noEmit": true
  },
  "include": ["**/*.ts"],
  "exclude": []
}
