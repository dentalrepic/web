---
// Croatian contact page (default locale, served from the site root).
import ContactPage from "../components/pages/ContactPage.astro";
import { defaultLocale } from "../i18n/config";
---

<ContactPage locale={defaultLocale} />
